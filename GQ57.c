//Find the sum of array elements.
#include <stdio.h>
int main()
{
    int array[]={2,4,6,8,10};
    int i, sum = 0;
    for (i = 0; i < 5; i++);
    sum = sum + array[i];
    
        printf("the sum of array elements is: %d", sum);
        return 0;
    
}